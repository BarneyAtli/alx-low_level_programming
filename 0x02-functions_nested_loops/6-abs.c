#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 *
 * Return: 1 if greater than 0, 0 if zer and -1 if less than 0
 */
int _abs(int)
{
	if (n < 0)
		return (n * -1);
	return (n);
}
